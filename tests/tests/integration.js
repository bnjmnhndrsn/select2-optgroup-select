module('Integration');

// test('Selecting an unselected optgroup selects all the child elements', function(){});
// test('Selecting a selected optgroup selects all the child elements', function(){});
// test('Selecting all of the child elements selects the optgroup', function(){});
// test('Unselecting one of the child elements unselects the optgroup', function(){});
