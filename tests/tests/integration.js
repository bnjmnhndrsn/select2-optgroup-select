module('Integration');

test('Selecting an unselected optgroup selects all the child elements');
test('Selecting a selected optgroup selects all the child elements');
test('Selecting all of the child elements selects the optgroup');
test('Unselecting one of the child elements unselects the optgroup');
